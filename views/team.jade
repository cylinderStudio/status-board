table#projects
	tr
		th(style='width:45px;')
		th Team Member
		th Status
	each el in team_statuses
		tr
			td.projectPersons
				img.person(style='margin-left:4px', src='/images/'+ el.id + '.png')
			td.projectName #{el.name}
			td.projectVersion #{el.status}